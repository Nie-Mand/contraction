{
  "name": "hardhat-project",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:dev": "hardhat run scripts/index.ts --network localhost",
    "deploy": "hardhat run scripts/index.ts --network matic"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "ethers": "^5.6.9",
    "hardhat": "^2.10.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.7.2",
    "bn.js": "^5.2.1",
    "dotenv": "^16.0.1"
  }
}
